services:
  compose-tool:
    image: ghcr.io/beacherz/compose-tool:latest
    container_name: compose-toolrestart: unless-stopped
    ports:
      - "8765:80"
    environment:
      - TZ=Asia/Shanghaihealthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
